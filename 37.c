#include<stdio.h>
int main()
{
    int i=1;
    while(i<10)
    {
        printf("Hello\n");
    }
    printf("Hi");
}

/// How many times Hi will be printed ? -> zero times
/// statements after infinite loop are not allowed in java
